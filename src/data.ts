// Cell values
const EMPTY = "E";
const CIRCLE = "O";
const CROSS = "X";

// Game data
let data = [EMPTY, EMPTY, EMPTY, EMPTY, EMPTY, EMPTY, EMPTY, EMPTY, EMPTY];

export { EMPTY, CIRCLE, CROSS, data };
